{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Formulaire d’inscription</h1>

    {{ form(form_premiere_inscription) }}
    {# {{ dump (form_premiere_inscription)}} #}

    {# {{ form_start(form_premiere_inscription)}}
        {{ form_row(form_premiere_inscription.prenomClient)}}
        {{ form_row(form_premiere_inscription.nomClient)}}
        {{ form_row(form_premiere_inscription.mailClient)}}
        {{ form_row(form_premiere_inscription.motDePasseClient)}}

    <ul class="client_premiere_inscription_form_adresse">
        {% for adresse in form_premiere_inscription.adresse %}
            <li>{{ form_row(adresse.numeroAdresse)}}</li>
        {% endfor %}
    </ul> #}
        {# <div class="col-sm-8" id="client_premiere_inscription_form_adresse" data-prototype="{{ form_widget(form_premiere_inscription.adresse.vars.prototype)|e('html_attr') }}">
            {% for row in form_premiere_inscription.adresse %}
                {{ form_row(row)}}
            {% endfor %}
        </div> #}
        {# {{ form_end(form_premiere_inscription)}} #}
    
</div>
{% endblock %}
